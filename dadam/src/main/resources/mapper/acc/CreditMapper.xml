<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dadam.acc.credit.mapper.CreditMapper">
	

	<!-- 거래처 여신 리스트 -->
	<!-- 가지고 올 수 있는 거래처 테이블 정보 :  거래처명/ 거래처코드/ 여신금액/ 수금금액/ 여신총액-수금금액 = 미수금/ 미수금 >=0 상태 / 
			전표 테이블 / 최근수금일자(전표) -->
	<select id="creditFindAll" resultType="com.dadam.acc.credit.service.CreditVO">
	SELECT v.vdr_name
	     , v.vdr_code
	     , v.biz_no
	     , (SELECT emp_name 
	          FROM employees e 
	         WHERE e.emp_id = v.emp_id) emp_id
    	 , v.credit_price
    	 , v.credit_bal_price
    	 , v.credit_period
    	 , get_credit_status(credit_bal_price, credit_price ) status
    	 , (SELECT MAX(c.update_date)
         	  FROM chit c
         	  JOIN orders o ON ord_code = article_code
             WHERE o.vdr_code = v.vdr_code
               AND c.status = 'cst04') AS createdDate
	  FROM vender v
	 WHERE v.com_id = #{comId}
	 ORDER BY v.vdr_code
	</select>
	
	<!-- 거래처별 여신 상세 목록 -->
	<select id="creditFindCode" resultType="com.dadam.acc.credit.service.CreditVO">
	SELECT c.article_code 
		 , v.credit_price
		 , c.tot_price
		 , v.credit_bal_price
		 , v.bank
		 , v.acct_no
		 , getSubCode(c.type) type
		 , getEmpName(v.emp_id, #{comId}) as emp_id 
		 , c.created_date
		 , getEmpName(c.phy_emp_id, #{comId}) as phy_emp_id 
		 , c.chit_code
	  FROM chit c
	  JOIN orders o ON ord_code = article_code
	  JOIN vender v ON o.vdr_code = v.vdr_code
	 WHERE o.vdr_code = #{vdrCode}
	   AND c.com_id = #{comId}
	   AND c.status = 'cst04'
	</select>


<!-- 

CASE
WHEN c.article_code LIKE 'txh-%' THEN
getVdrName((SELECT vdr_code FROM vender WHERE acct_no = 
		 (SELECT acct_no FROM transaction WHERE txn_hist_code = 
		 (c.article_code ))))
WHEN c.article_code LIKE 'ord-%' THEN
getVdrName(SELECT) 
  ELSE ''
END AS vdrCode,

 -->
	<!-- 거래처원장 검색 -->
	<select id="creditSearch" resultType="com.dadam.acc.credit.service.CreditVO">
	  SELECT * FROM credit
	  WHERE com_id = #{comId}
	  <if test="vdrName != null and vdrName != ''">
	    AND vdr_name LIKE '%'||#{vdrName}||'%'
	  </if>
	  <if test="vdrCode != null and vdrCode != ''">
	    AND vdr_code LIKE '%'||#{vdrCode}||'%'
	  </if>
	  <if test="arrears != null and arrears != ''">
	    AND arrears LIKE '%'||#{arrears}||'%'
	  </if>
	  <if test="empId != null and empId != ''">
	    AND emp_id LIKE '%'||#{empId}||'%'
	  </if>
	  ORDER BY vdr_code DESC
	</select>

</mapper>