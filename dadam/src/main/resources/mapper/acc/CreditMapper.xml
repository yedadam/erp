<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dadam.acc.credit.mapper.CreditMapper">
	
	<!-- 거래처 여신 리스트 -->
	<!-- 가지고 올 수 있는 거래처 테이블 정보 :  거래처명/ 거래처코드/ 여신금액/ 수금금액/ 여신총액-수금금액 = 미수금/ 미수금 >=0 상태 / 
			전표 테이블 / 최근수금일자(전표) -->
	<select id="creditFindAll" resultType="com.dadam.acc.credit.service.CreditVO">
	SELECT v.vdr_name
	     , v.vdr_code
    	 , v.credit_period
    	 , v.credit_price
    	 , v.credit_bal_price
    	 , get_credit_status(v.credit_bal_price) status
    	 , c.created_date
	  FROM vender v
	  JOIN chit c ON v.vdr_code = c.vdr_code
	 ORDER BY v.vdr_code
	</select>
	
	
	<!-- 거래처별 여신 상세 목록 -->


</mapper>